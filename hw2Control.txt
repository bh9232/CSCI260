#alloc k->$s0, m->$s1, s->$s2
add $s0,$s1,$zero		#k<-m+0
Loop:sll $t0,$s1,1	#t0<-2m
slt $t1,$t0,$s0		#t1<-1 if 2m<k; else 0
beq $t1,$zero,End		#exit loop if 2m!<k;
andi $t2,$s0,1		#t2<-1 if odd; else 0
beq $s0,1,Else		#goto Else if odd
add $s2,$s2,$s0		#s<-s+k
addi $s0,s0,1		#k<-k+1
Else:add $s2,$s2,-1	#s<-s-1
sll $s2,$s2,3		#s<-s*8
addi $s0,$s0,-1		#k<-k-1
j Loop			#jump to for loop
End:add $s2,$s2,$s0	#s<-s+k